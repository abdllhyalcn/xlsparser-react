const ReqURL = 'http://localhost:8080';
const defaultTimeout = 5000;

const defaultError = 'An Error Occured!';

const apiUsername = 'username';
const apiPassword = 'pass12';

export default {
    ReqURL,
    defaultTimeout,
    defaultError,
    apiUsername,
    apiPassword
};